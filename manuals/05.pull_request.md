# Pull Requestを送る

Pull Requestを送り、CI/CDによるステージング環境を利用する。

1. GitHub上の **自分のFork** （`https://github.com/<your-account>/genai-mkdocs-hands-on`）のページを開く
2. `Contribute` → `Open pull request` を選択する

しばらくするとDeployされますが、少々時間がかかるので一旦先に別の作業を進めましょう。